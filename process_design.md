# Process Design: Daily Trends Pipeline

_Generated by automation-designer on 2026-05-22. Updated 2026-05-26 to reflect locked design decisions and Slice A scope (GitHub issue #1)._

## 1. Context

A trend-discovery pipeline that runs daily and surfaces AI-focused content the operator can turn into Substack / LinkedIn / Instagram posts. It pulls content from four eventual source families:

(a) **News** — Hacker News front page and TechCrunch's AI category, fetched live by a subagent.
(b) **Vendor blogs** — Anthropic (`claude.com/blog`) and OpenAI Developers (`developers.openai.com/blog`) plus any future first-party AI vendor blogs, fetched by a separate subagent.
(c) **Instagram Reels** — scraped from a curated creator list and transcribed locally via faster-whisper. *Deferred — see "Implementation Slices" below.*
(d) **X posts** — scraped from a curated creator list. *Deferred — see "Implementation Slices" below.*

All collected content is clustered into topics by an LLM subagent, which produces `trending_topics.json` plus per-topic content recommendations for the operator's publishing channels (configurable; currently Substack, LinkedIn, Instagram). The output is delivered to the operator (`mariogj1987@gmail.com`) via email and saved to disk under a versioned `runs/<run_id>/` directory.

## 1a. Implementation Slices

The full pipeline ships in three additive slices. Each slice is a working end-to-end pipeline; later slices add sources without restructuring shared modules.

**Slice A — News + Vendor Blogs (initial implementation, PRD: GitHub issue #1).**
- Sources: News subagent (HN + TC AI) and Vendor blogs subagent (Anthropic + OpenAI Devs).
- All downstream steps (normalize → cluster → recommendations → email) wired and tested.
- Two orchestration skills shipped: `run-trends` and `recluster`.

**Slice B — Instagram Reels.**
- Bright Data Web Scraper Dataset API for metadata + yt-dlp for download.
- faster-whisper transcription with auto-detect language + translate-to-English when non-English.
- `creators/accounts.json[instagram]` populated.

**Slice C — X posts.**
- Bright Data X Posts Dataset API.
- `creators/accounts.json[x]` populated.
- Resolves Open Question on date-range listing capability.

Each slice extends the same `CorpusNormalizer` module additively (more readers, no other module changes) and the same `config.json` (more keys: `instagram_lookback_days`, `x_lookback_days`, etc.). Subagent prompts (clustering, recommendations) do not change between slices — they operate on the unified corpus schema.

## 2. Objective

Produce, per run, two JSON artifacts — `trending_topics.json` (topic groupings + summaries + other-notable items) and `content_recommendations.json` (per-channel ideas per topic) — and email the operator an HTML digest plus a structured error summary.

**Must-haves**

- **No silent failures.** Errors, warnings, and consequential-info events are logged in detail to `runs/<run_id>/errors.log`; the email's "Errors & Skips" section gives a structured Python-rendered summary so missing data is never invisible.
- **Config-driven.** `creators/accounts.json` (which accounts — populated in slices B/C) and `config.json` (lookback windows, email_mode, content channels, clustering threshold, vendor blog list) are editable without touching pipeline code.
- **Outputs are versioned, never overwritten.** Each run writes to a fresh `runs/<UTC-timestamp>/` directory. Cross-run duplication is acceptable.
- **Graceful slow-day behavior.** If normalized corpus < `min_corpus_for_clustering` (default 4), the pipeline skips clustering + recommendations and renders a "light signal" email branch. An email always lands, so inbox status reflects pipeline status.
- **Confidence-gated delivery.** `email_mode` config flag (`"draft" | "send"`) lets the operator preview rendered emails as Gmail drafts before flipping to direct send.

## 3. Process Map

Slice A active steps drawn solid; slices B/C steps shown for context.

```
[ 0. Pre-flight: init_run.py — create runs/<run_id>/, validate env ]
        |
        v
        +--------+------------+------------+--------------------------+
        |        |            |            |                          |
        v        v            v            v                          v
[1. IG Reels] [2. Whisper] [3. X posts] [4a. News subagent]   [4b. Vendor-blogs subagent]
  (slice B)   (slice B)    (slice C)     - WebFetch                  - WebFetch
                                         - HN front + TC AI          - claude.com + openai devs
                                         - subagent AI filter        - lookback 7d
                                         - no lookback config        - first-party canonical
                                              (concurrent with 4b)        (concurrent with 4a)
        |        |            |            |                          |
        +--------+------------+------------+--------------------------+
                                |
                                v
                  [ 5. Normalize corpus (Python: CorpusNormalizer) ]
                    - unify schema across sources
                    - drop word_count < 30 (logged as consequential-info)
                    - write corpus.json
                                |
                                v
                  [ 5a. Slow-day branch (orchestrator) ]
                    - if len(corpus) < min_corpus_for_clustering:
                        write skipped_clustering.json
                        SKIP steps 6 + 7 -> go to step 8 (light-signal email)
                                |
                                v
              [ 6. Topic-clustering subagent (fresh context) ]
                - adaptive topic count; min 2 items per topic
                - items not clustering -> other_notable[]
                - write trending_topics.json
                                |
                                v
              [ 7. Recommendations subagent (fresh context) ]
                - one idea per channel in content_channels
                - rationale per topic
                - no voice/brand filter
                - write content_recommendations.json
                                |
                                v
                  [ 8. Deliver email (Python: EmailRenderer + GmailSender) ]
                    - render templates/email_template.html (topic cards)
                    - alt render path: light-signal branch when skipped
                    - Gmail API via google-api-python-client
                    - email_mode: draft | send
                    - attach both JSONs (omitted on slow-day)
                    - include Python-rendered errors summary
```

## 4. Materials and Capabilities per Step

Slice A rows are marked **[A]**; slice B with **[B]**; slice C with **[C]**.

| Step | Inputs (Materials) | Outputs (Materials) | Capabilities (Tools) | Awareness Check |
|------|--------------------|---------------------|----------------------|-----------------|
| **[B]** 1. Scrape IG Reels | `config.json` (ig_lookback_days), `creators/accounts.json[instagram]` | Per Reel: `<post_id>.mp4` + `<post_id>.meta.json` | Python + Bright Data Web Scraper API + `yt-dlp` | yt-dlp returned URL; log+skip on fail. API uses `post_type=reel`. Empty account = info log. |
| **[B]** 2. Transcribe Reels | `<post_id>.mp4` from step 1 | Per Reel: `<post_id>.transcript.json` (`{text, text_en?, language, duration_sec}`) | Python + `faster-whisper` (small, int8); PyAV-bundled ffmpeg | Auto-detect language; non-English also gets `task="translate"`. try/except each call -> errors.log + skip. |
| **[C]** 3. Scrape X posts | `config.json` (x_lookback_days), `creators/accounts.json[x]` | `x/<account>.json` containing `[{post_id, account, posted_at, text, url}]` | Python + Bright Data X Posts Dataset API | Empty account = info log. Pending: verify date-range listing capability. |
| **[A]** 4a. News-search subagent | `prompts/news_search_prompt.md` (static) | `news/articles.json` — `[{url, title, source, published_at, summary}]` | Agent tool (general-purpose, Sonnet) + WebFetch | Fetch `news.ycombinator.com/` and `techcrunch.com/category/artificial-intelligence/`. Apply AI-relevance judgment per item. No date config — "what's there now" is the window. |
| **[A]** 4b. Vendor-blogs subagent | `prompts/vendor_blogs_prompt.md` (static), `config.vendor_blogs[]`, `config.vendor_blogs_lookback_days` (default 7) | `vendor_blogs/posts.json` — same schema as news | Agent tool (general-purpose, Sonnet) + WebFetch | First-party canonical — include all posts in window, no relevance gate. Runs concurrently with 4a. |
| **[A]** 5. Normalize corpus | All available step 1-4 outputs | `corpus.json` — `[{id, source, account_or_outlet, posted_at, text, url}]` | Python (CorpusNormalizer) | Drop records `word_count < 30`; log dropped count as consequential-info. |
| **[A]** 5a. Slow-day branch | `corpus.json`, `config.min_corpus_for_clustering` | `skipped_clustering.json` (when triggered) | Orchestrator (skill body) | If `len(corpus) < threshold`, skip steps 6+7; route to light-signal email render path. |
| **[A]** 6. Cluster topics | `corpus.json`, `prompts/clustering_prompt.md` | `trending_topics.json` — `{topics: [{topic_id, topic_name, description, conversation_summary, member_ids}], other_notable: [{id, title, url, one_line}]}` | Agent tool (general-purpose, Sonnet, fresh context) | Adaptive count; minimum 2 items per topic. Items that don't cluster cleanly land in `other_notable`. No ranking, no scoring. |
| **[A]** 7. Recommendations | `trending_topics.json`, `prompts/recommendations_prompt.md`, `config.content_channels` | `content_recommendations.json` — `[{topic_id, ideas: {<channel>: idea_text, ...}, rationale}]` | Agent tool (general-purpose, Sonnet, fresh context) | One idea per channel listed; rationale per topic; no voice/brand filter. |
| **[A]** 8. Deliver email | `trending_topics.json`, `content_recommendations.json`, error summary, `templates/email_template.html`, `config.email_mode` | Email drafted/sent; `runs/<run_id>/email_sent.html` archived | Python (EmailRenderer + GmailSender); google-api-python-client (Gmail API, `gmail.send` scope) | Topic-card layout. Errors section is Python deterministic summary (error/warning always shown; info shown if `kind: "consequential"`). Light-signal alt render when `skipped_clustering.json` present. |

**Cross-cutting awareness contract.** Every step appends structured JSON lines to `runs/<run_id>/errors.log` via the `ErrorLog` module. Schema: `{step, severity, item_id?, message, timestamp, kind?}` where `severity ∈ {"error","warning","info"}` and optional `kind: "consequential"` promotes info-level facts (like drop counts) into the email summary. Non-fatal errors never abort the run; uncaught script errors do abort.

## 5. Workspace Layout

Local repo at `C:\Users\MarioPC\Apps\daily-trends\`. Code is version-controlled; per-run state lives under `runs/` and is gitignored.

```
daily-trends/
├── config.json                     email_to, email_mode, content_channels[],
│                                   vendor_blogs[], vendor_blogs_lookback_days,
│                                   min_corpus_for_clustering
│                                   (slices B/C add: instagram_lookback_days, x_lookback_days)
├── creators/
│   └── accounts.json               { instagram: [...], x: [...] }  (populated in slices B/C)
├── .env                            (slices B/C: BRIGHT_DATA_API_TOKEN)
├── .gitignore                      ignores runs/, .env, credentials/, *.mp4
├── requirements.txt                slice A: requests, python-dotenv,
│                                   google-api-python-client, google-auth-oauthlib,
│                                   google-auth-httplib2
│                                   slices B/C add: faster-whisper, yt-dlp
├── README.MD
├── process_design.md               this file — canonical eventual-target design
├── prd-slice-a.md                  Slice A PRD (mirror of GitHub issue #1)
│
├── .claude/
│   ├── skills/
│   │   ├── run-trends/SKILL.md     Slice A orchestrator (extends per-slice)
│   │   └── recluster/SKILL.md      Re-run steps 6-8 against existing corpus
│   └── (Matt Pocock skill collection)
│
├── credentials/                    (gitignored)
│   ├── oauth_client.json           one-time download from Google Cloud Console
│   └── token.json                  generated on first OAuth run; auto-refreshes
│
├── scripts/                        Python entry points called by orchestrator
│   ├── init_run.py                 pre-flight; prints run_id to stdout
│   ├── normalize_corpus.py         wraps CorpusNormalizer
│   ├── send_email.py               wraps EmailRenderer + GmailSender
│   ├── lib/                        deep modules (RunWorkspace, ErrorLog,
│   │                               CorpusNormalizer, EmailRenderer, GmailSender)
│   └── (slices B/C add: scrape_instagram.py, scrape_x.py, transcribe_reels.py)
│
├── prompts/                        static prompts; orchestrator appends config inline
│   ├── news_search_prompt.md
│   ├── vendor_blogs_prompt.md
│   ├── clustering_prompt.md
│   └── recommendations_prompt.md
│
├── templates/
│   └── email_template.html         topic-card layout; conditional light-signal branch
│
├── tests/                          pytest
│   ├── test_corpus_normalizer.py
│   ├── test_email_renderer.py
│   ├── test_error_log.py
│   └── test_gmail_sender.py        unit (MIME) + integration (draft mode)
│
└── runs/                           one folder per run; never overwritten
    └── 2026-05-22T08-00/           run_id = UTC ISO timestamp, minute precision
        ├── news/articles.json
        ├── vendor_blogs/posts.json
        │   (slices B/C add: instagram/<account>/{<post_id>.mp4, .meta.json, .transcript.json}, x/<account>.json)
        ├── corpus.json
        ├── skipped_clustering.json (only on slow days)
        ├── trending_topics.json    (absent on slow days)
        ├── content_recommendations.json (absent on slow days)
        ├── email_sent.html
        ├── lineage.json            (only in /recluster runs)
        └── errors.log
```

## 6. Harness and Model Decisions

**Capability → harness tool mapping**

| Capability | Harness tool |
|------------|--------------|
| Orchestrate sequence, manage run_id, route errors | Claude Code main session (via `/run-trends` skill) |
| Re-run synthesis steps against existing corpus | Claude Code main session (via `/recluster` skill) |
| Find + summarize trending news | Fresh-context subagent via Agent tool (WebFetch) |
| Find + summarize vendor blog posts | Fresh-context subagent via Agent tool (WebFetch) |
| Deterministic data transforms (normalize, render, dispatch) | Python deep modules invoked via Bash entry-point scripts |
| LLM synthesis (clustering, recommendations) | Fresh-context subagents via Agent tool (general-purpose) |
| Send HTML email with attachments | Python (GmailSender), google-api-python-client (Gmail API, scope `gmail.send`) |
| (Slice B) Scrape IG metadata + filter videos | Python + Bright Data Web Scraper API |
| (Slice B) Download Reel `.mp4` binary | yt-dlp invoked from Python |
| (Slice B) Local speech-to-text + translation | `faster-whisper` (small, int8) in Python |
| (Slice C) Scrape X posts per account per date range | Python + Bright Data X Posts Dataset API |

**Harness:** Claude Code main session as orchestrator. Skill-based entry (`.claude/skills/run-trends/SKILL.md` and `.claude/skills/recluster/SKILL.md`). Reasoning: the workflow blends deterministic Python with LLM judgment stages and benefits from Claude Code's ability to drive both via Bash and Agent tools without leaving the local environment. State lives on local disk; secrets stay in `.env` and `credentials/` outside any cloud harness. Migration to a headless Python orchestrator using the Anthropic SDK is anticipated for scheduled production runs (Open Question — Cadence), but the skill body is designed to translate mechanically.

**Concurrency:** Source-fetch subagents (news + vendor_blogs in slice A; potentially news + vendor_blogs + IG-metadata-fetch in later slices) are spawned in a single tool block to execute concurrently. Clustering and recommendations remain sequential (strict dependency).

**Prompt-config wiring:** Prompt files in `prompts/` are static text. The orchestrator reads each prompt at invocation time, appends relevant config values as prose, and passes the combined string to the Agent tool. No templating engine.

**Model:** **Sonnet 4.6** for all subagents (news, vendor_blogs, clustering, recommendations). The rule of thumb is the least intelligence that does the job reliably — these are retrieval + light summarization + bounded ideation, not requiring Opus-level reasoning. Easy upgrade path: change the `model` parameter on a single Agent call to `opus` later if topic quality is weak. The orchestrator itself runs as Opus 4.7 because it handles error routing across all stages.

## 6a. Module Breakdown

Deep modules (encapsulate logic behind a simple interface that rarely changes; testable in isolation):

- **RunWorkspace** — owns run lifecycle. `new_run()` generates a UTC-minute-precision run_id and creates the directory tree. `existing_run(run_id)` resolves paths against an existing run for `/recluster`. Exposes typed paths for every file the pipeline reads or writes.
- **ErrorLog** — append-only JSON-lines writer plus a reader producing a grouped `ErrorSummary`. Supports `severity ∈ {error, warning, info}` and optional `kind: "consequential"` for info entries that affect downstream output (promoted into the email summary).
- **CorpusNormalizer** — single deterministic transform that reads all per-source JSON inputs, unions into the corpus schema, applies `word_count < 30` filter, logs the dropped count. Adding IG transcripts or X posts in later slices is a new reader inside this module — no other module changes.
- **EmailRenderer** — reads `trending_topics.json` + `content_recommendations.json` + error summary, joins topics and recommendations by `topic_id`, substitutes into the HTML template. Two render paths: full (topic cards + Other Notable + Errors) and light-signal (Other Notable + Errors + corpus-size note). Selected by reading whether `skipped_clustering.json` is present in the workspace.
- **GmailSender** — wraps `google-api-python-client`. Encapsulates OAuth load + token refresh, multipart/mixed MIME assembly (HTML body + JSON attachments), and dispatch by mode (`drafts.create` for `email_mode: "draft"`; `messages.send` for `"send"`). Returns a `SendResult`.

Thin composition layer (entry-point scripts and skill bodies): `init_run.py`, `normalize_corpus.py`, `send_email.py`, plus the two skill markdown files. No logic worth testing in isolation; exercised by end-to-end dry runs.

**Tests** (pytest):
- `CorpusNormalizer`: fixture-driven; schema correctness, drop filter, log entries, idempotency, empty-input edge cases.
- `EmailRenderer`: synthetic inputs; topic-card ordering, Other Notable formatting, Errors section, light-signal branch, extra-channel resilience.
- `ErrorLog`: append behavior, summary grouping, severity counts, consequential-info promotion.
- `GmailSender`: unit test on MIME assembly (no network) + integration against the real Gmail API in draft mode (creates draft, asserts presence, deletes for cleanup). `mode="send"` not auto-tested.

## 7. Skill / Agent Instructions (Slice A)

_Body of `.claude/skills/run-trends/SKILL.md`._

```
You are the orchestrator for the daily-trends pipeline (Slice A). Drive the
sequence below end-to-end. Do not aggregate, count, or transform data in your
head — pipe to Python scripts. Surface all errors to errors.log and the final
email.

PRE-FLIGHT (every run)

1. Run `python scripts/init_run.py` via Bash. Capture run_id from stdout.
   The script creates runs/<run_id>/{news,vendor_blogs}/ and writes an empty
   errors.log. It also validates: config.json present, credentials/
   oauth_client.json present. Validation failure -> non-zero exit + stderr
   message -> abort the pipeline.
2. Read config.json. Echo a one-line summary to console: run_id, email_mode,
   vendor blog count, slow-day threshold.

STEP 4 — Source subagents (CONCURRENT)

3. Read prompts/news_search_prompt.md and prompts/vendor_blogs_prompt.md.
4. In a single tool block, spawn TWO subagents concurrently:
   - News subagent (Agent tool, general-purpose, Sonnet, fresh context,
     WebFetch). Prompt = <news_search_prompt.md contents> + the hardcoded
     AI-only filter instructions + "Fetch news.ycombinator.com/ and
     techcrunch.com/category/artificial-intelligence/. Return JSON array
     [{url, title, source, published_at, summary}]."
   - Vendor-blogs subagent (same harness). Prompt = <vendor_blogs_prompt.md
     contents> + the vendor_blogs[] list from config + "Lookback
     {{vendor_blogs_lookback_days}} days; include ALL posts in window — no
     relevance filter (first-party canonical)."
5. Save outputs to runs/<run_id>/news/articles.json and
   runs/<run_id>/vendor_blogs/posts.json respectively.
6. Subagent failure -> ErrorLog + continue. Empty output -> warning + continue.

STEP 5 — Normalize corpus

7. Run `python scripts/normalize_corpus.py --run-id <run_id>` via Bash.
   Wraps CorpusNormalizer; writes corpus.json; logs drop count as
   consequential-info.

STEP 5a — Slow-day check

8. Read corpus.json. If len(corpus) < config.min_corpus_for_clustering:
   - Write runs/<run_id>/skipped_clustering.json with {reason, corpus_size}.
   - SKIP steps 6 and 7. Jump to step 8.

STEP 6 — Clustering subagent

9. Read prompts/clustering_prompt.md. Spawn subagent (Agent tool,
   general-purpose, Sonnet, fresh context) with prompt + corpus.json
   contents inlined.
10. Output schema: {topics: [{topic_id, topic_name, description,
    conversation_summary, member_ids}], other_notable: [{id, title, url,
    one_line}]}. Adaptive topic count; min 2 items per topic; singletons
    -> other_notable.
11. Save to runs/<run_id>/trending_topics.json.

STEP 7 — Recommendations subagent

12. Read prompts/recommendations_prompt.md. Spawn subagent with prompt +
    trending_topics.json contents + the literal content_channels list
    from config.
13. Output schema: [{topic_id, ideas: {<channel>: idea_text, ...},
    rationale}]. One idea per listed channel; rationale per topic; no
    voice/brand filter.
14. Save to runs/<run_id>/content_recommendations.json.

STEP 8 — Deliver email

15. Run `python scripts/send_email.py --run-id <run_id>` via Bash.
    Internally: EmailRenderer.render(workspace, config) -> HTML (full or
    light-signal branch); archive to runs/<run_id>/email_sent.html;
    GmailSender.dispatch(html, attachments, mode, to) per config.email_mode.
    Attachments: trending_topics.json, content_recommendations.json
    (omitted on slow-day runs).
    Subject: [daily-trends] Run <run_id>.

16. Echo final summary line: Run <run_id> complete. Topics: N (or
    "slow-day"). Ideas: M. Errors: K. Email outcome: <draft|sent>.

GLOBAL RULES

- Prefer Python scripts for any calculation, aggregation, or deterministic
  transformation. LLM math/counting is unreliable.
- Never overwrite outputs from prior runs. Run-id'd folders are immutable.
- errors.log is append-only.
- All API keys/credentials come from .env or credentials/. Never echo secrets.
- On any uncaught script error, abort the remaining pipeline. Surface the
  failing step to console.
```

_Body of `.claude/skills/recluster/SKILL.md` (summary)._

Takes `<source_run_id>` as argument. Calls `init_run.py` to create a new run; copies `corpus.json` from the source run; writes `lineage.json` `{source_run_id, reused: ["corpus.json"], created_at}`; runs steps 5a (slow-day check), 6 (cluster), 7 (recommendations), 8 (email) of `run-trends`. New run is fully self-contained; the source run is untouched.

## 8. Dry-Run Checklist (Slice A)

Before trusting the automation, verify:

- [ ] **Minimal-config smoke test.** Run `/run-trends` with default config (`email_mode: "draft"`). Expected: pipeline completes; `runs/<run_id>/` populated; draft arrives in Gmail with topic-card layout; vendor blog with no posts in 7d appears as info-level note, not error.
- [ ] **Slow-day branch.** Temporarily set `min_corpus_for_clustering: 1000` to force trigger. Re-run. Expected: `skipped_clustering.json` present; `trending_topics.json` absent; email renders light-signal alternate layout (Other Notable + Errors only). Revert config.
- [ ] **Failure-injection.** Temporarily change a vendor blog URL to a 404. Expected: failure lands in `errors.log` with `step: "vendor_blogs"`; rest of pipeline continues; email's Errors section reflects it. Restore config.
- [ ] **Recluster cycle.** Pick a recent successful run, invoke `/recluster <id>`. Expected: new run directory with `lineage.json` pointing at source; `corpus.json` byte-identical to source; clustering + recommendations + email re-execute cleanly.
- [ ] **Run-immutability check.** Invoke `/run-trends` twice within a few minutes. Expected: two separate `runs/<TS>/` folders; neither modifies the other.
- [ ] **Subagent output schema check.** After a successful run, validate `trending_topics.json` and `content_recommendations.json` parse and contain documented fields.
- [ ] **Email mode promotion.** Once `"draft"` outputs look right after ~3-5 runs, flip `email_mode: "send"`. Verify next run delivers directly to inbox.

**Future dry-runs (slices B/C):** Non-English Reel transcription + translation; bad Reel URL / revoked token; config-sensitivity for `instagram_lookback_days`; X account+date listing.

**Explicitly out of scope for the dry-run:** subagent output quality grading (judged by reading, iterated via prompt edits); scheduling cadence + how to trigger.

## 9. Open Questions

**Resolved by grill-me session 2026-05-26:**

- ~~Email send vs draft mode.~~ → `email_mode` config flag with values `"draft" | "send"`, default `"draft"`. Operator flips after 3-5 confidence runs.
- ~~News selection rule + lookback.~~ → Live HN front page + TechCrunch AI category via WebFetch. No date-bounded API. "What's there now" is the implicit window. `news_lookback_days` removed from config.
- ~~Vendor blogs handling.~~ → Separate concurrent subagent with own prompt and lookback (`vendor_blogs_lookback_days`, default 7). First-party canonical — no relevance filter.
- ~~Clustering granularity.~~ → Adaptive count; minimum 2 items per topic; singletons land in `other_notable` bucket. Slow-day floor at `min_corpus_for_clustering` (default 4) skips clustering entirely.
- ~~Recommendations channel list.~~ → Configurable `content_channels[]` (default `["substack", "linkedin", "instagram"]`). Schema uses keyed `ideas` map for non-breaking channel additions; rationale field retained.
- ~~Topic-clustering output shape.~~ → `{topics: [...], other_notable: [...]}` (was flat array).
- ~~Email layout.~~ → Topic-card (per-topic block contains description + summary + members + ideas + rationale). Other Notable then Errors as tail sections.
- ~~Errors summary mechanism.~~ → Python deterministic (no subagent); error/warning always shown; info shown only when `kind: "consequential"`.
- ~~Orchestrator shape.~~ → Two custom skills (`run-trends`, `recluster`). Headless Python orchestrator deferred until scheduling is decided.
- ~~Topical filter for news.~~ → AI-only, hardcoded into the news subagent prompt.

**Still open:**

- **Bright Data X Dataset API account+date listing.** Verify against Bright Data docs before writing `scrape_x.py` in Slice C; if not supported, plan a fallback (search-by-username with date filter, or a different X scraper).
- **Cadence + trigger mechanism.** Slice A is manual-trigger only. Pick daily vs. other cadence + Windows Task Scheduler vs. manual `claude` invocation after first stable run. Likely requires migration to headless Python orchestrator at that point.
- **Whisper hardware path.** Determine CPU vs. GPU for Slice B based on local hardware; update `requirements.txt` accordingly (CUDA wheels vs. CPU).
- **Cost monitoring on Bright Data.** Free tier is 5,000 requests/month; track usage when slices B/C land.
