# Secrets
.env
.env.*
!.env.example
credentials/

# Node
node_modules/
dist/
build/
*.log
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Editors / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Apify / Crawlee local storage
apify_storage/
storage/
crawlee_storage/

# Output / scratch
output/
data/raw/
data/cache/
*.tmp
_tmp/

# Pipeline run artifacts (per-run output trees)
runs/

# Model caches (faster-whisper weights downloaded via WhisperModel(download_root=...))
models/

# Claude local config — ignore everything except the project's own skills
.claude/*
!.claude/skills/
.claude/skills/*
!.claude/skills/run-trends/
!.claude/skills/recluster/
skills-lock.json

# Docs — ignore scratch dumps at the root of docs/, keep structured subdirs (ADRs, agent guides)
docs/*
!docs/adr/
!docs/agents/